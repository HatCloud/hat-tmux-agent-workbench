# Changelog

- **2026-07-17**: Grok Build 作为第三 agent client 接入 + `internal/agentclient` Adapter 注册表。窗名/状态/🔔、launcher fzf、workspace 7 列 dual-read（client+session_key）；修复 busy 时 queue 假 `[?]`、陈旧 `@agent_client` 导致 Window Nav Provider 误标 Codex。daemon WatchHints 接线；文档 ARCHITECTURE/GUIDE/README。（HAT-596）
- **2026-07-16**: 修复 tmux.conf shell 注入 RCE（15 处 run-shell/command-prompt format 变量补 `#{q:}`，`%%`→`%%%`）+ daemon 健壮性（finishTask 锁外探测消除 TOCTOU、tracker-server 8 处 exec 加 3s 超时、new_session.sh 标志位 trap+陈旧超时自愈）。源自 2026-07-15 全项目审计（HAT-561 副产物）唯一 Critical + 2 条 Important。codex review 全程参与（design 1 轮、plan 5 轮、code review 2 轮），抓出 4 个真实并发/边界 bug（acknowledge 覆盖、Acknowledged 初值陷阱、lsappinfo stderr 污染、未来 mtime 边界）。移交 `#()` command-substitution 注入面（可能更高危、未验证）至 HAT-587。（HAT-586）
- **2026-07-15**: 开源化改造完成并发布：`~/.hat-config` 重生为公开仓 `hat-tmux-agent-workbench`（GitHub `HatCloud` 账号），新增交互式部署向导（双语/依赖/图标/键位）、三套图标集、项目内 statusline 实现，修复 iTerm2 状态栏 ⍰ 渲染 bug；双机（本机+mini）迁移完成并回归通过。（HAT-561）
- **2026-06-29**: SSH 远程嵌套 tmux + agent-tracker 全栈支持。让本地这套工作流在「本地外层嵌套远程内层」SSH 场景可用：F12 passthrough（外层 `set key-table off` 透传内层、橙底 `⌨ PASSTHROUGH` 指示、`C-q` 二次逃逸）、ssh 窗自动 `🌐 host` 标记（`parseSSHHost` flag 感知解析 + `sshProcessArgs` 进程子树 BFS + sanitize 防注入 + 退出清理交还自动命名）、嵌套 status 错位（per-machine 固定：内层 `top` / 外层 `bottom`，`update_status_position.sh` 显式值不被 ssh 检测覆盖）、含 ssh pane 的外层窗自动隐藏 pane-border 标题（`reconcileSSHPaneBorder`）、mini 远程 `deploy.sh install` 部署验证 + `docs/GUIDE.md`「SSH 远程嵌套」段（tmux ≥ 3.3）。P5 实机反馈追加 mini tmux 3.2a→3.7、status 方向反转、pane-border 让位。全量 code review C=0 I=0。（HAT-534）
- **2026-06-22**: Timer 面板独立化 + Snippet 内容库升级。Snippet 升级为带目录分组 / `.favorites` 收藏 / 面板内原生 CRUD / `{{var}}` 变量的内容库（`snippet.go` 数据层 + `snippet_panel.go` hjkl 面板，两种消费模式）。Timer 面板独立化绑 `prefix t`（原 todos → `prefix T`），同屏三栏 This Window/Other Windows/History（仿 todo，`Tab` 切焦点）；Other Windows 行带状态(●busy/○idle/?asking)+名字+目录、过滤已结束 window；per-window 历史（去重/重建/`s` 存入 snippet `timer` 组）、Add 表单 `Ctrl+P` 从 snippet 取内容；trigger 支持复合时长 `3h20m`。新增 `docs/GUIDE_TIMER_SNIPPET.md`。code review 2 轮 C=0 I=0，含 2 个 Revise（全局视图 → 同屏三栏）。（HAT-523）
- **2026-06-20**: 修复 agent-tracker 在等待后台任务/subagent 返回时误发「✅ 任务已完成」通知——daemon `finishTask` 加宽限去抖（首次 idle 仅置 `PendingCompleteAt` 不通知，持续 idle 满 `completionGraceWindow`(2s) 才提交完成；活动信号清 pending）、poller `reconcileActions` 识别 `shell` 活动态（Claude 结束 turn 但有后台任务在跑时写的状态）保持 in_progress 不触发完成。新增 daemon/poller 单测，文档同步。（HAT-515）
- **2026-06-20**: 剔除 agent-tracker 内从未接线的 agent-workspace 重机制死代码（registry/repo-copy/bootstrap/Chrome 浏览器自动化/Flutter device/palette Tracker·Devices 面板/Start agent，main.go 瘦身 ~3300 行 + 删 5 文件），共享 helper（updateAppConfig/sendTrackerCommand/trackerLoadState）迁移到保留文件；子命令面收缩为 `agent <palette|windows|tmux|tracker|ime>`。原拟吸收的 worktree 隔离（`agent -i`）经验收用户判定无用、整体撤回，任务最终为纯剔除。保留 daemon 任务追踪、Window Nav、tmux workspace 存档零回归。（HAT-513）
- **2026-06-19**: 还原并增强 tmux + agent-tracker 工作流——agent 启动器（fzf 选 client/provider + 三 pane + `-r/--resume`）、窗口名锚定 Claude 会话标题并实时同步（含 `[B]/[I]` 状态前缀、client/provider 实时探测、窗口编号、目录/client/provider 缩写）、状态走 sessions-json 轮询 + daemon 对账 🔔、session 切换 F1-F9、palette Vim j/k 导航；launchd daemon + `claude mcp add` + hooks 经 deploy.sh 一键部署。P2/P3 agent 工作空间注册延后（Linear HAT-513）。
